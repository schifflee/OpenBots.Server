import { PreventSpecialCharDirective } from './prevent-special-char.directive';

describe('PreventSpecialCharDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventSpecialCharDirective(null);
    expect(directive).toBeTruthy();
  });
});
